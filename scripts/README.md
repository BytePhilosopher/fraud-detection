# Scripts

Runnable entry points and code generators.

| Script | Purpose |
|---|---|
| `build_notebooks.py` | Generate all `notebooks/*.ipynb` deterministically with `nbformat`. Keeps notebook source diff-friendly and reproducible. |

```bash
python scripts/build_notebooks.py
```
